# Minimal FastAPI requirements for quick deployment
fastapi==0.115.0
uvicorn[standard]==0.30.0
python-multipart==0.0.9
pydantic==2.9.0
gunicorn==21.2.0
python-dotenv==1.0.0

# Basic dependencies only
requests==2.32.3
Pillow==10.4.0
numpy==1.26.4

# Skip heavy dependencies for now - add back after successful deployment
# supabase==2.3.4  # Causing dependency conflicts
# torch==2.4.0+cpu  # Too heavy for initial deployment
# torchvision==0.19.0+cpu
# opencv-python-headless==4.10.0.84
# matplotlib==3.9.0
# tqdm==4.66.4
# aiohttp==3.10.0